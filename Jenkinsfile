pipeline {
    agent any
    stages{
      stage ('Compile Stage') {
        steps{
          sh 'echo Compile'
        }

      }
      stage ('Testing Stage') {
        steps{
          sh 'echo Test'
        }

      }
      stage ('Deployment Stage') {
        steps{
          sh 'echo Deploy'
          }

      }
    }
}
